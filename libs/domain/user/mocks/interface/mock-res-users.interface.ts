import { IUser } from "../../../../shared/entity";

/**
 * @export
 * @interface
 * @name IMockResUsers
 * @property { IUser[] } users
 * @see { IUser }
 */
export interface IMockResUsers {
  users: IUser[];
}
