import { MockResProfile } from "./mock-res-profile";
import { IProfile } from "../../../../shared/entity";

/**
 * @export
 * @const { MockResUpdateProfile }
 * @name MockResUpdateProfile
 * @see { IProfile, MockResProfile }
 */
export const MockResUpdateProfile: IProfile = MockResProfile;
